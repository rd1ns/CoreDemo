@using EntitiyLayer.Concrete
@model List<Blog>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/UserLayout.cshtml";
}

<section class="main-content-w3layouts-agileits">
    <div class="container">
        <h3 class="tittle">KOMBİ MODELLERİ</h3>
        <div class="inner-sec">
            <!--left-->
            <div class="left-blog-info-w3layouts-agileits text-left">
                <div class="row">
                    @foreach (var item in Model)
                    {
                        <div class="col-lg-4 card">
                            <a href="/Blog/BlogReadAll/@item.BlogID/">
                                <img src="@item.BlogImage" class="card-img-top img-fluid" alt="">
                            </a>
                            <div class="card-body">
                                <ul class="blog-icons my-4">
                                    <li>
                                        <a href="#">
                                            <i class="far fa-calendar-alt"></i> @(((DateTime)item.BlogCreateDate).ToString("dd-MMM-yyyy"))

                                        </a>
                                    </li>
                                    <li class="mx-2">
                                        <a href="#">
                                            <i class="far fa-comment"></i> 21
                                        </a>
                                    </li>
                                    <li>
                                        <a href="#">
                                            <i class="fas fa-eye"></i> @item.Category.CategoryName
                                        </a>
                                    </li>

                                </ul>
                                <h5 class="card-title">
                                    <a href="/Blog/BlogReadAll/@item.BlogID">@item.BlogTitle</a>
                                </h5>
                                <p class="card-text mb-3">@item.BlogContent.Substring(0,50)</p>
                                <a href="/Blog/BlogReadAll/@item.BlogID/" class="btn btn-primary read-m">Devamını Oku..</a>
                            </div>
                        </div>
                        <br />
                    }
                </div>
                <script>

                    function notifyMe() {
                        if (!("Notification" in window)) {
                            alert("This Browser Not System");
                        }
                        else if (Notification.permission === "granted") {
                            notify();
                        }
                        else if (Notification.permission !== 'denied') {
                            Notification.requestPermission(function (permission) {
                                if (permission === "başarılı") {
                                    notify();
                                }
                            });
                        }
                        function notify() {
                            var notification = new Notification('Bildirimleriniz Açık', {
                                /*icon: "aaa.jfif",*/
                                body: " Rd1ns Youtube Channel ",

                            });
                            notification.onclick = function () {
                                window.open("https://www.udemy.com/user/enes-ridvan-gundogan/")
                            };
                            setTimeout(notification.close.bind(notification), 70000);
                        }
                        aaa.jfif
                    }
                    notifyMe();

                </script>
            </div>
        </div>
    </div>

</section>
@*.Substring(0, item.BlogContent.Substring(0, 89).LastIndexOf(""))*@